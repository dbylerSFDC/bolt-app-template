import { App } from "@slack/bolt";
import { HomeView } from "$home";

export class HomeRender {
  // constructor(app: App) {
  //   app.event("app_home_opened", async ({ event, body, client }) => {
  //     this.renderHomeTab(app, event.user);
  //   });
  // }

  // renderHomeTab(app: App, user: string) {
  //   new HomeView().publishHome(app, user);
  // }
}
